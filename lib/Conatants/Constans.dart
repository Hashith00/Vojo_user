import 'dart:ui';

// Diffing all the colors
const Color primaryColor = Color(0xFF311B92);
const Color secoundaryColor = Color(0xffffffff);

// Diffing the font Sizes
const int primaryTitleTextSize = 20;
const int normalTextSize = 16;

// Diffing the font weights
const FontWeight primaryTitleFontWeight = FontWeight.w500;

// Diffing the font families
const String primaryFontFamilty = "Readex Pro";

